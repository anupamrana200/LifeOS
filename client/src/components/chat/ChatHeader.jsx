import { Maximize2, Menu, Minimize2, PanelRightOpen } from 'lucide-react';

export const ChatHeader = ({ conversation, isExpanded, isFullscreen, isStreaming, onInfoOpen, onSidebarOpen, onToggleExpanded, onToggleFullscreen, selectedModel, selectedProvider }) => (
  <header className="flex min-h-16 items-center justify-between gap-3 border-b border-border px-gutter py-3">
    <div className="flex min-w-0 items-center gap-2"><button aria-label="Open conversations" className="inline-flex size-9 shrink-0 items-center justify-center rounded-control text-content-secondary hover:bg-canvas lg:hidden" onClick={onSidebarOpen} type="button"><Menu aria-hidden="true" size={19} /></button><div className="min-w-0"><h2 className="truncate font-display text-sm font-semibold text-content-primary">{conversation?.title || 'New conversation'}</h2><div className="mt-1 flex min-w-0 items-center gap-2"><span className="truncate rounded-pill bg-primary/10 px-2 py-0.5 text-xs font-medium text-primary">{conversation?.provider || selectedProvider}</span><span className="hidden truncate text-xs text-content-muted sm:inline">{conversation?.model || selectedModel}</span>{isStreaming && <span aria-live="polite" className="text-xs font-medium text-primary">Generating…</span>}</div></div></div>
    <div className="flex items-center gap-1"><button aria-label={isExpanded ? 'Restore chat panels' : 'Expand chat workspace'} aria-pressed={isExpanded} className="hidden size-9 items-center justify-center rounded-control text-content-secondary hover:bg-canvas lg:inline-flex" onClick={onToggleExpanded} type="button">{isExpanded ? <Minimize2 aria-hidden="true" size={18} /> : <Maximize2 aria-hidden="true" size={18} />}</button><button aria-label={isFullscreen ? 'Exit full screen' : 'Open full-screen chat'} className="hidden size-9 items-center justify-center rounded-control text-content-secondary hover:bg-canvas sm:inline-flex" onClick={onToggleFullscreen} type="button">{isFullscreen ? <Minimize2 aria-hidden="true" size={18} /> : <Maximize2 aria-hidden="true" size={18} />}</button><button aria-label="Open conversation details" className="inline-flex size-9 items-center justify-center rounded-control text-content-secondary hover:bg-canvas xl:hidden" onClick={onInfoOpen} type="button"><PanelRightOpen aria-hidden="true" size={18} /></button></div>
  </header>
);
