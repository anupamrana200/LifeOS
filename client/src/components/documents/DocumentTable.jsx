import { formatDocumentDate, formatFileSize } from '@/utils';
import { DocumentActionsMenu } from './DocumentActionsMenu';
import { DocumentStatusBadge } from './DocumentStatusBadge';

export const DocumentTable = ({ documents, ...actions }) => <div className="overflow-x-auto rounded-panel border border-border"><table className="w-full min-w-[44rem] text-left text-sm"><thead className="bg-canvas text-xs uppercase tracking-wide text-content-muted"><tr><th className="px-gutter py-3">Document</th><th className="px-gutter py-3">Type</th><th className="px-gutter py-3">Size</th><th className="px-gutter py-3">Uploaded</th><th className="px-gutter py-3">Status</th><th className="px-gutter py-3" /></tr></thead><tbody className="divide-y divide-border bg-card">{documents.map((document) => <tr key={document.id}><td className="max-w-xs px-gutter py-3"><p className="truncate font-medium text-content-primary">{document.title}</p><p className="truncate text-xs text-content-muted">{document.originalFileName}</p></td><td className="px-gutter py-3 text-content-secondary">{document.mimeType?.split('/').pop()?.toUpperCase()}</td><td className="px-gutter py-3 text-content-secondary">{formatFileSize(document.fileSize)}</td><td className="px-gutter py-3 text-content-secondary">{formatDocumentDate(document.createdAt)}</td><td className="px-gutter py-3"><DocumentStatusBadge document={document} /></td><td className="px-gutter py-3"><DocumentActionsMenu document={document} {...actions} /></td></tr>)}</tbody></table></div>;
