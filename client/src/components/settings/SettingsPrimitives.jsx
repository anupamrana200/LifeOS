export const SettingsSection = ({ children, description, title }) => <section className="rounded-panel border border-border bg-card p-gutter shadow-card"><h2 className="font-display text-lg font-semibold text-content-primary">{title}</h2>{description && <p className="mt-1 text-sm text-content-secondary">{description}</p>}<div className="mt-gutter space-y-4">{children}</div></section>;
export const ToggleSetting = ({ checked, description, label, onChange }) => <label className="flex cursor-pointer items-center justify-between gap-4"><span><span className="block text-sm font-medium text-content-primary">{label}</span>{description && <span className="mt-1 block text-xs text-content-secondary">{description}</span>}</span><input aria-label={label} checked={checked} className="size-5 accent-primary" onChange={(event) => onChange(event.target.checked)} type="checkbox" /></label>;
export const SelectSetting = ({ label, onChange, options, value }) => <label className="block text-sm font-medium text-content-primary">{label}<select className="mt-1.5 w-full rounded-control border border-border bg-surface px-3 py-2 text-sm" onChange={(event) => onChange(event.target.value)} value={value}>{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label>;
export const SliderSetting = ({ label, max, min, onChange, step, value }) => <label className="block text-sm font-medium text-content-primary">{label}<span className="ml-2 text-content-muted">{value}</span><input className="mt-3 w-full accent-primary" max={max} min={min} onChange={(event) => onChange(Number(event.target.value))} step={step} type="range" value={value} /></label>;
export const PreferenceCard = ({ children }) => <div className="rounded-card bg-canvas p-3">{children}</div>;
